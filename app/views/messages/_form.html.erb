<%= form_for @message, remote: true, html: { class: 'messages__form' } do |f| %>
  <%= f.text_area :body, class: 'messages__form__input' %>
  <%= f.submit 'Send', class: 'messages__form__button' %>
<% end %>
